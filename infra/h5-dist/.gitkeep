# Sprint 12: uniapp build:h5 产物 deploy 目标. 由 .github/workflows/deploy.yml
# 的 H5 build job scp 到 ECS /opt/xgzh/h5-dist/, caddy 挂载 :ro serve.
# 本目录 git 只保留占位 .gitkeep, 真实 build 产物不入 git (在 .gitignore 里)。
