---
description: 金融合规与文案护栏（最高优先级）
alwaysApply: true
---

# 合规与文案护栏（最高优先级 🚨）

> XGZH 严格定位为 **「信息聚合工具」**，不构成投资 / 税务 / 法律建议。
> 完整合规设计见 `spec/06-商业化变现与合规避险.md`。

## 1. 关键词黑名单（用户可见的任何文案与 AI 输出）

以下词汇 **绝对禁止** 出现在 UI 文案、Prompt、AI 输出中：

```
建议(满仓|重仓|全仓|加仓|抄底)
强烈(推荐|建议)买入
必涨 / 稳赚 / 包赚 / 躺赚
保本 / 保收益 / 无风险
all in / 梭哈
打新必中 / 中签率 100%
```

**违规处理流程**：
- 代码中若发现这类文案 → 立即停下来跟用户确认
- AI 输出层面：通过 `forbidden_pattern_filter()` 自动重写
- Prompt 层面：所有 system prompt 必须显式约束

## 2. 必备免责声明

### 2.1 AI Agent 输出末尾（每条都要）

```
> ⚠️ 以上分析仅供参考，不构成投资建议，请独立决策。
```

后端实现：`ensure_disclaimer()` 自动追加，禁止依赖 LLM 自觉。

### 2.2 全局页面（必须固定可见）

| 页面 | 必带文案 |
|------|---------|
| AI Agent 对话页 | 顶部："AI 输出仅供参考，不构成投资建议" |
| 新股详情页 | 底部："数据来源：XX，最终以官方文件为准" |
| 券商对比页 | 底部："信息可能滞后，开户/交易请以券商官方为准" |
| CRS 向导 | 每页："本工具仅作流程参考，最终请咨询持牌专业人士" |
| 个税计算器 | "估算结果仅供参考" |
| 历史数据图表 | "历史业绩不代表未来表现" |

### 2.3 注册时（用户协议必含）

- 不构成投资 / 税务 / 法律建议
- 不接收用户资金 / 不代持账户 / 不撮合交易
- AI 内容可能存在错误，以官方文件为准
- 因依赖本服务而产生的损失，平台不承担责任

## 3. 业务红线（绝对不做）

```
❌ 接受用户资金 / 代持账户
❌ 出具个性化投资建议（"建议你买 XX"）
❌ 推荐具体证券买卖时点
❌ 提供保本 / 保收益类承诺
❌ 运营"打新群""荐股群"
❌ 销售证券 / 基金 / 保险 / 虚拟货币
❌ 提供税务申报代办（无会计/税务师资格）
```

如果代码 / Prompt 中出现以上任何一种意图 → **立刻停下，不要写**，跟用户确认。

## 4. 数据合规（PIPL / GDPR）

### PII 处理

- 收集最小化：仅收必要字段
- 日志中**禁止**完整手机号 / 身份证 / 银行卡（脱敏：`138****5678`）
- 数据导出 / 删除接口必须实现
- 第三方 SDK 必须列示用途，可关闭

### 数据出境

- 境内用户数据存境内服务器
- CRS 类内容若涉及境外查阅 → 用户额外授权 checkbox

## 5. 内容版权（文章模块）

- **只存摘要 + 原文链接**，不存全文
- 必标注来源（来源 logo + 名称 + 时间）
- 用户上传内容（远期 UGC）必须机审 + 人审
- 收到投诉 24h 内下架

## 6. 平台政策

| 平台 | 关键约束 |
|------|---------|
| 微信小程序 | "金融-理财信息咨询"类目；不能跳外部小程序支付；不出现敏感词 |
| App Store | 订阅必须 Apple IAP；不能引导外部支付 |
| Google Play | 个人信息保护合规；权限最小化 |

## 7. 工程层强制实现

每个面向用户的 LLM 调用必须经过这个 pipeline：

```python
output = llm.generate(prompt)
output = validate_citations(output, retrieved_docs)   # 引用源校验
output = forbidden_pattern_filter(output)             # 黑名单过滤
output = ensure_disclaimer(output)                    # 免责声明
return output
```

**禁止**绕过任何一环。
