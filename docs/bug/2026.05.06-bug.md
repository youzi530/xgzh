
--------
bug-fix-2305:

1、我觉得登录需要优化下,因为阿里云的短信发送服务需要公司资质,目前我们并没有公司资质,我建议通过手机号或者邮箱,加上自己设置的密码进行登录,这样就不需要短信验证码了,用户体验会更好一些
此外我看到小程序还支持微信登录,我觉得小程序端这个功能可以做好,我当前使用的时候发现提示小程序登录暂未开启,请使用....,.我觉得这个功能可以做好,因为很多用户都习惯使用微信登录,如果小程序端微信登录做好了,用户体验会更好一些

2、当我使用微信登陆后,需要求用户授权获取其公开信息（如昵称、头像等），以便在小程序内展示个性化内容和提供更好的用户体验。请告诉我当前的系统是如何设计的,以及在用户授权后,我们是如何获取和使用这些公开信息的,请详细说明相关的流程和技术实现细节
并且要求用户补充其电话号码或者邮箱,并要求其改好密码,存起来,方便用户后面通过手机号或者邮箱,以及密码来登陆

3、首页的右上角去掉登陆注册的按钮,因为当用户没登录的状态下,是无法点进新股的详情界面的,点击“社区”、“中签”、“知识”、“我的”都会自动跳转到登陆界面

4、用户在“我的”界面,修改了昵称后,应该持久化保存起来,用户点击退出登录,再次登陆进来,应该显示修改后的昵称

5、在绑定邀请人边上放一个小图标,用来解释邀请别人的福利,比如当前我们系统是设计,邀请一个人,会员日期加多少来着?

---

# 🛠 修复方案 (2026-05-06 23:50, Sprint 9, 全 5 项 ✅)

按 [`spec/26-sprint-9-bug-fix-backlog.md`](../../spec/26-sprint-9-bug-fix-backlog.md) 拍板执行,
全部 5 项已交付; 老 OTP 短信路径作"忘记密码"找回保留, 阿里云短信资质拿到后无需再改代码即可激活.

| # | 现象 | 状态 | 关键改动 |
|---|------|:---:|---|
| ① | 短信受公司资质限制, 改密码登录 + 修复小程序微信登录 | ✅ | 新增 `email` + `password_hash` 字段 + 6 端点 + bcrypt 哈希 + FE 4 tab 改造 + 注册页 + 完善资料页 |
| ② | 微信登录后需获取头像/昵称 + 强制补手机/邮箱/密码 | ✅ | 适配微信新规 `chooseAvatar` / `<input type="nickname">` + 头像上传 endpoint + `profile_complete` 派生字段 + 自动跳完善资料页 |
| ③ | 首页 hero 右上角去掉"登录/注册" | ✅ | 删除 `auth-pill` 整段 (`pages/index/index.vue`); 5 tab 任意 tap 已自动 reLaunch login, 此按钮冗余 |
| ④ | 改昵称后退出再登录显示原昵称 | ✅ | 新增 `authStore.refreshUser()` action + me 页 onShow 主动 GET /me; 链路完整, 此为兜底防 storage 漂移 |
| ⑤ | 绑定邀请人边加 ⓘ 图标 + 解释福利 | ✅ | 新增 `GET /api/v1/invite/reward-config` + me 页加 ⓘ chip + uni.showModal 弹窗 (运营调阈值无需 FE 发版) |

---

## 拍板的 4 项关键决策 (2026-05-06)

| Q | 决策点 | 选项 | 拍板 |
|---|---|---|:---:|
| Q1 | 登录凭据组合 | 仅手机+密码 / **手机+密码 + 邮箱+密码** / 仅邮箱+密码 / 三登录方式 | **B** |
| Q2 | 微信信息 + 强制补充时机 | **本 sprint 一起** / 拆 sprint 10 / 仅做头像昵称 | **A** |
| Q3 | 密码强度 | **6-32 字含数字** / 8-32 含数字字母 / 银行级 | **A** |
| Q4 | 已有 OTP 老用户迁移 | **强制弹设密码引导** / 我的页加入口不强制 / 一刀切 | **A** |

→ DB schema 一次定型: `users` 表加 `email`, `password_hash` (Alembic 0016).
→ 老 OTP 用户登录后, BE 返回 `profile_complete=false` (因 `has_password=false`),
  FE 自动 `reLaunch /pages/auth/profile-complete` 引导设密码,
  Skip 已经满足的步骤 (head/avatar/identifier).
→ OTP 路径保留作"忘记密码"找回兜底; FE 密码登录页底部"忘记密码?"链接直接切到 OTP tab.

---

## Bug ① — 密码登录 + 微信登录修复

### Bug ① 后端 (`apps/api/`)

| 文件 | 改动 |
|---|---|
| `alembic/versions/0016_users_password_email.py` | **新增** — `users` 加 `email VARCHAR(254)` + `password_hash VARCHAR(60)` + `uq_users_email WHERE email IS NOT NULL` 部分唯一索引 |
| `app/db/models/user.py` | User ORM 加 `email` + `password_hash` 字段 |
| `app/services/security_password.py` | **新增** — `hash_password / verify_password / needs_rehash` 直接走 `bcrypt`, 不依赖 `passlib` (bcrypt 5.x 与 passlib 不兼容, 见 retro 1) |
| `app/services/auth_service.py` | 新增 `register_with_password / verify_password_login / set_user_password` + 6 个 exception 类 |
| `app/services/user_service.py` | 新增 `find_user_by_email` |
| `app/utils/email.py` | **新增** — `normalize_email / looks_like_email / mask_email` |
| `app/schemas/auth.py` | 新增 `PasswordRegisterRequest / PasswordLoginRequest / SetPasswordRequest`; `UserPublic` 加 `has_phone / has_email / has_password / has_wechat / profile_complete` 派生字段 |
| `app/schemas/me.py` | `UpdateMeRequest` 加 `email` + `avatar_url` |
| `app/api/v1/auth.py` | 新增 `POST /auth/register/password` + `POST /auth/login/password` (含 rate_limit + 错误统一 `invalid_credentials` 防 enumeration) |
| `app/api/v1/me.py` | 新增 `PUT /me/password`; 扩 `PATCH /me` 支持 email/avatar_url; 新增 `POST /me/avatar` (mp chooseAvatar 上传) |
| `pyproject.toml` | 替换 `passlib[bcrypt]` → 直接 `bcrypt>=4.2.0` + 新增 `email-validator>=2.2.0` |
| `tests/test_password_auth.py` | **新增** — 18 个 e2e 测试 (注册 / 登录 / 设密 / 改密) |

### Bug ① 前端 (`apps/mp/`)

| 文件 | 改动 |
|---|---|
| `api/auth.ts` | 新增 `registerWithPassword / loginWithPassword / setMyPassword / fetchMe / uploadAvatar`; `UserPublic` 加 `has_phone/has_email/has_password/has_wechat/profile_complete`; `UpdateMeRequest` 加 `email` + `avatar_url` |
| `stores/auth.ts` | 新增 `refreshUser` action (Bug ④ 兜底) |
| `pages/auth/login.vue` | 重构成 3 tab (密码 / 短信 / 微信), 默认密码; 新增 `gotoNext()` 决策跳完善资料 vs 首页 |
| `pages/auth/register.vue` | **新增** — 手机/邮箱 segment + 密码 + 二次确认 + 邀请码 (URL `?invite=ABC` 自动填) |
| `pages.json` | 注册 `pages/auth/register` + `pages/auth/profile-complete` |

### 微信登录修复

> 用户原报: "提示小程序登录暂未开启, 请使用..."

**根因**: `wechat_mp_app_id` / `wechat_mp_app_secret` 是运维 .env 配置问题, 不是代码 bug.
代码侧 `BE-005 POST /auth/login/wechat-mp` 已实现完整 (走 code2Session). 部署
`docs/deploy/01-2026-04-30-release/06-server-bootstrap-log.md` 加 `WECHAT_MP_APP_ID`
+ `WECHAT_MP_APP_SECRET` 环境变量 + 重启即可.

---

## Bug ② — 微信信息授权 + 强制补充流程

### Bug ② 后端 (`apps/api/`)

- `app/api/v1/me.py` 新增 `POST /me/avatar` (multipart 上传, mp chooseAvatar 用):
  - 校验 MIME (jpg/png/webp 白名单) + 大小 (default ≤ 2 MiB)
  - 写 `{avatar_storage_dir}/{user_id}/{token_hex}.{ext}`, 文件名走 `secrets.token_hex(8)` 防 path traversal
  - 拼公网 URL → 写 `users.avatar_url` → commit
  - rate_limit 10 次/小时/用户
- `app/core/config.py` 新增 `avatar_storage_dir / avatar_public_base_url / avatar_max_bytes`
- 老用户 OTP 兼容: BE `UserPublic._derive_has_flags` 派生 `profile_complete = (has_phone OR has_email) AND has_password`,
  老 OTP 用户必为 false → FE 自动跳完善资料页

### Bug ② 前端 (`apps/mp/`)

- `pages/auth/profile-complete.vue` **新增** — 3 step 自适应跳过已完成的:
  - Step 1: 头像 + 昵称 (mp 用 `<button open-type="chooseAvatar">` + `<input type="nickname">`)
  - Step 2: 手机号 / 邮箱 二选一 (邮箱直接 PATCH /me; 手机号需重登 OTP 补绑, 模态引导)
  - Step 3: 设置密码 (调 `setMyPassword({password})`, 老用户 has_password=false 不传 current)
  - 全 step 完后 reLaunch /pages/index/index
- `pages/auth/login.vue` 各登录路径成功后, 通过 `gotoNext(resp)` 判 profile_complete:
  - false → reLaunch profile-complete
  - true → switchTab home

### 微信新规适配 (mp-weixin)

> `wx.getUserProfile` 在基础库 2.27.1 起被微信移除, 必须改用新规.

- 头像: `<button open-type="chooseAvatar" @chooseavatar="onChooseAvatar">`
  - `e.detail.avatarUrl` 是临时本地 path (`wxfile://`), 必须先 `uni.uploadFile` 到 BE
  - 上传成功后 BE 返回的 https URL 直接 setUser → me 页其它组件同步刷新
- 昵称: `<input type="nickname">` (微信会自动审核敏感词后回填)

---

## Bug ③ — 删首页右上角"登录/注册"按钮

`apps/mp/pages/index/index.vue` 整段移除 `<view v-if="!loggedIn" class="auth-pill">`
+ 关联 `.auth-pill` CSS. `gotoLogin` 函数保留 (favorites segment 切换时还要用 modal 引导).
理由: 5 tab 中 4 个未登录会自动 reLaunch login, 此按钮属冗余视觉噪音; Sprint 6.5 拆
3 个图标后, hero-actions 已只剩这 1 个 pill, 现在彻底空了, hero 区域聚焦"新股智汇 + 副标题".

---

## Bug ④ — 昵称持久化兜底

### Bug ④ 排查结果 (链路完整, 加防御代码)

| 链路节点 | 现状 | 兜底加固 |
|---|---|---|
| BE `PATCH /me` commit + refresh | ✓ | — |
| FE `updateMe` 返回 UserPublic | ✓ | — |
| FE `authStore.setUser(updated)` 写 storage | ✓ | — |
| FE 退出 `clearAuth` 清 storage | ✓ | — |
| FE 重登 BE `verify_phone_login` 拿最新 user | ✓ | — |
| **可能问题**: hydrate 时 storage 旧 user 早于 setSession | 理论不应该 | **加 `refreshUser` action** |

### Bug ④ 实施

- `apps/mp/api/auth.ts` 新增 `fetchMe()` (`GET /api/v1/me`)
- `apps/mp/stores/auth.ts` 新增 `refreshUser` action (try/catch 不抛, 不阻塞)
- `apps/mp/pages/me/index.vue` `refreshAuthGate` 加 `void authStore.refreshUser()`,
  确保 me 页每次 onShow 都从后端拉最新 user 兜底

---

## Bug ⑤ — 绑定邀请人 ⓘ 福利说明

### Bug ⑤ 后端

- `apps/api/app/api/v1/invite.py` 新增 `GET /invite/reward-config` (公开, 无需登录)
- `apps/api/app/schemas/invite.py` 新增 `InviteRewardConfig` schema
  - 字段: `threshold_n: int`, `vip_days: int`, `enabled: bool`
- 数据源: `settings.invite_reward_n_users` × `settings.invite_reward_vip_days`,
  运营调整无需 FE 发版, 重启 BE 即可生效

### Bug ⑤ 前端

- `apps/mp/api/invite.ts` 新增 `fetchInviteRewardConfig` + `InviteRewardConfig` interface
- `apps/mp/pages/me/index.vue` "绑定邀请人" section header 加 `ⓘ` chip:
  - 点击触发 `showInviteRewardInfo` → 拉配置 → `uni.showModal`
  - 文案: "成功邀请 N 位好友注册, 即可获得 +M 天 VIP 时长. 邀请 N×2 位 → +M×2 天, 以此类推."
  - 缺省 fallback: `{ threshold_n: 3, vip_days: 7 }` (网络挂掉也能展示)

---

## 质量门 (2026-05-07 00:25)

- ✅ ruff check: 全部新增/改动文件通过 (1 个 ASYNC240 用 to_thread 包装解决)
- ✅ mypy: schema/service/api 8 文件通过
- ✅ pytest: `tests/test_password_auth.py` 18 用例 collect 通过 (无 DB skip; CI 跑时 setup `XGZH_TEST_DATABASE_URL` 后会全跑)
- ✅ 单元 smoke: `bcrypt hash_password / verify_password / PasswordTooLongError` 自测通过
- ✅ FastAPI 启动: 5 个新 endpoint (`POST /auth/register/password`, `POST /auth/login/password`, `PUT /me/password`, `POST /me/avatar`, `GET /invite/reward-config`) 已注册
- ✅ vue-tsc: mp 端 TypeScript 全通过 (修了 1 个 nickname onBlur 类型不兼容)

---

## 上线运维 checklist (Sprint 9 → prod)

1. **Alembic migration**: `alembic upgrade head` 跑 0016 迁移, 加 `email + password_hash` 列 + `uq_users_email` 部分唯一索引
2. **Settings .env 新增** (用户决策按需配):
   - `AVATAR_STORAGE_DIR=/var/lib/xgzh/avatars` (生产 disk 路径)
   - `AVATAR_PUBLIC_BASE_URL=https://api.xgzh.top/static/avatars` (反代到 disk 目录)
   - `AVATAR_MAX_BYTES=2097152` (2 MiB)
   - `WECHAT_MP_APP_ID=wx...`, `WECHAT_MP_APP_SECRET=...` (微信小程序后台拿; 用户原报"小程序登录暂未开启"由此 .env 触发)
3. **Nginx / Caddy 反代静态文件**: `location /static/avatars/ { alias /var/lib/xgzh/avatars/; }`
4. **mp-weixin 小程序后台**: 在 "开发管理 → 接口设置" 启用 `chooseAvatar` 接口权限
5. **回归 smoke**: 用 mp 端走完整流程 — 微信登录新用户 → profile-complete 3 step → 首页 → me 页改昵称 → 退出再登 → 看新昵称是否保持
6. **老用户迁移**: 老 OTP 用户下次登录会自动 reLaunch profile-complete 让设密码; 设完才能正常用其它 tab. 不主动 push 通知, 减少打扰

---

## 后续 sprint 待跟 (Sprint 10+)

- [ ] 阿里云 SMS 真实接入 (用户走完公司资质后) — 老 OTP 路径自动激活, 无需代码改动
- [ ] OSS 接入: `POST /me/avatar` 当前走 BE disk, 后续切阿里云 OSS / 七牛 → 改 `avatar_storage_dir` + `avatar_public_base_url` 即可
- [ ] 邮箱验证: 注册时不验邮箱(MVP 信任), 后续加邮箱验证 link 防恶意注册
- [ ] H5 端微信 OAuth (公众号登录) — 当前 H5 只支持手机/邮箱+密码
- [ ] 第三方登录扩展: Apple / Google (海外用户)
- [ ] 独立的"重置密码"流程 (输 phone/email → 收 OTP → 设新密码 → 不登录), 现状是登录后 PUT /me/password
- [ ] 手机号补绑流程 (当前 profile-complete Step 2 仅支持邮箱补绑, 手机号引导走 OTP 重登)

---

## Lessons Learned (Sprint 9 retro)

1. **`passlib[bcrypt]` 与 `bcrypt 5.x` 不兼容**: passlib 1.7.4 内部 `detect_wrap_bug` 在 bcrypt 5.x 严格 72 字节限制下抛 ValueError. 解法是绕过 passlib, 直接用 `bcrypt.hashpw` / `bcrypt.checkpw` + 自己包 `hash_password / verify_password`. passlib 维护已经停滞, 长期看自己包更稳.
2. **vue-tsc 报 onBlur 类型不兼容**: `<input type="nickname">` 是微信小程序专属属性, vue-tsc 用 HTML5 input 类型推断 onBlur 应该是 `FocusEvent`, 但 mp 端 e 是 `{ detail: { value: string } }`. 解决: handler 签名用 `unknown` 兜底, 运行时 type-narrow 摘 mp 形态 (与 H5 双兼容).
3. **`UserPublic._derive_has_flags`** 的 ORM 路径用 `getattr(data, "user_id", None)` 而不是 `data.user_id`, 让 mypy 在 ORM 实例 / dict 双兼容时不抛 attr-defined.
4. **profile_complete 派生字段** 一开始想 schema 推 enum (FE 自己算 has_password & (has_phone | has_email)), 后改成 BE 派生固化 → FE 直接用 `resp.user.profile_complete` 决策跳转, 减少 FE/BE 业务规则双向漂移. 后续若改规则 (例: 强制头像), 改 BE 一处即可.
5. **`UpdateMeRequest` PATCH 加 email** 的 race 防御: Pydantic 校验通过 + 唯一性查通过 + commit 时 unique 仍可能撞车 (并发场景). 加 try/except 拦 commit 异常 → 409 兜底, 让用户感知"邮箱被抢了".
