
--------
bug-fix-2305:

1、目前我的代码上传到git上后,该怎么自动化同步到我的服务器上,请告诉我有没有更好的方式,比如说通过git hook的方式,当我push代码到git上后,自动化同步到服务器上,如果有的话,请告诉我具体的操作步骤和需要注意的事项

2、当我代码经过某种自动化校验,确定代码没问题后,如何在服务器上自动部署好,请告诉我有没有更好的方式,比如说通过CI/CD的方式,当我push代码到git上后,自动化校验代码,如果校验通过了,自动化部署到服务器上,如果有的话,请告诉我具体的操作步骤和需要注意的事项

✅ **完成 (2026-04-30 12:05–13:30)** — Sprint 9 部署体系交付

**方案** (用户拍板 B 主流 GitHub Actions + SSH + Docker 镜像驱动 + 阿里云 ACR):

```
GitHub push → ci.yml (fast + integration 现有) → deploy.yml (新增) → 阿里云 ACR push 镜像 → SSH 到 ECS 8.130.156.2 → docker compose pull + up -d → /healthz 健康检查 (5 retry) → 失败自动 rollback
```

**4 方案对比** (详见 [`spec/25-deployment-strategy.md`](../../spec/25-deployment-strategy.md) §"方案对比"):
- A. Server post-receive git hook — 最简但无校验门禁,**❌ 不选**
- **B. GitHub Actions + SSH + Docker — 标准 CI/CD,主流推荐, ✅ 用户拍板**
- C. Watchtower (镜像驱动) — 黑盒无 SSH 可观测性,❌ 不选
- D. K8s + ArgoCD — 大型项目,单服务器过早,❌ 不选

**关键二级决策**:
- 服务器 2G 内存极限 → 用户拍板 **A 不部署 Meili** (BE+PG+Redis 三容器 ~1.4G 安全)
- 镜像仓库 → 用户拍板 **阿里云 ACR** (国内同区拉取 100MB/s, 0 限额, 0 费用)
- 部署 scope → 用户拍板 **be_only** (BE+DB/Redis,FE H5/小程序留 Sprint 9 ext)

**5 文件交付**:
| 文件 | 类型 | 行数 | 说明 |
|---|---|:---:|---|
| [`spec/25-deployment-strategy.md`](../../spec/25-deployment-strategy.md) | DOC 新增 | ~340 | 完整方案 + 架构图 + 时序图 + 5 retro lessons |
| [`infra/docker-compose.production.yml`](../../infra/docker-compose.production.yml) | INFRA 新增 | ~100 | BE+PG+Redis 3 容器 + PG 内存调优 + 内存上限 + healthcheck |
| [`.github/workflows/deploy.yml`](../../.github/workflows/deploy.yml) | CI 新增 | ~150 | workflow_run 监听 ci.yml 全绿 → buildx → ACR push → SSH → 5 retry healthcheck → 失败 rollback |
| [`infra/server-setup.sh`](../../infra/server-setup.sh) | OPS 新增 | ~180 | 服务器一次性 setup (Docker + ufw + 1G swap + fail2ban + deployer 用户 + 阿里云 Docker mirror) |
| [`infra/.env.production.example`](../../infra/.env.production.example) | OPS 新增 | ~50 | 生产 .env 模板 (PG_PASSWORD/JWT_SECRET/LLM keys/LONGBRIDGE_TOKEN, 已确认 .env 在 .gitignore) |

**用户 follow-up checklist (~30 min 一次性)**:
- [ ] 阿里云 ACR 控制台创建命名空间 `xgzh` + 仓库 `api` + 访问凭证 (5 min)
- [ ] 本机 `ssh-keygen -t ed25519 -f ~/.ssh/xgzh_deploy` 生成部署 SSH key (3 min)
- [ ] 服务器 `ssh root@8.130.156.2` → wget `server-setup.sh` → bash 执行 (10 min)
- [ ] 把 deployer 公钥写入 `/home/deployer/.ssh/authorized_keys` (~2 min)
- [ ] 服务器 `cp .env.production.example .env` + `vi .env` 填密码 + JWT_SECRET + LLM keys (5 min)
- [ ] GitHub repo Settings → Secrets 配 8 个 secret(详见 spec/25 § Step 4 表)(5 min)
- [ ] 首次手动起 `docker compose up -d postgres redis` (服务器, ~30s)
- [ ] 本机 `git commit --allow-empty -m "deploy: 首次部署" && git push origin main` (CI+deploy ~10 min, 无人值守)

**安全提醒** (详见 spec/25 § "安全注意事项"):
1. 用 `deployer` 用户 SSH (不用 root) — setup 脚本已创建
2. 禁用 SSH 密码登录,只允许 key — 上线前 `vi /etc/ssh/sshd_config`
3. 阿里云安全组只放通 22/80/443/8000 — 5432/6379 仅 docker 内网通信
4. `.env` **绝对不进 git** — 已确认 `.gitignore` 第 2/61 行屏蔽
5. ACR_PASSWORD/SSH_KEY 每 6 月轮换

**2G ECS 内存规划** (详见 spec/25 § "ECS 内存规划"):
| 组件 | 实际占用 | 容器上限 |
|---|---|---|
| 系统 + Docker | ~340M | - |
| BE FastAPI (单 worker) | ~480M | 700M |
| PostgreSQL 16 (shared_buffers=128M) | ~450M | 600M |
| Redis 7 (maxmemory=200M) | ~80M | 250M |
| swap 兜底 | 0 (备用) | 1G |
| **总计** | **~1.35G** | 留 0.65G headroom |

**后续 (待用户拍板)**:
- Sprint 9 ext: FE H5 自动构建 + rsync 到服务器 Nginx 目录
- Sprint 10: 多服务器水平扩展 + SLB (用户量 > 1000 DAU 后)
- 可选: 部署通知到 Slack/钉钉 webhook
- 可选: 域名 + HTTPS (阿里云域名 → certbot Let's Encrypt → Nginx 反代 8000)




